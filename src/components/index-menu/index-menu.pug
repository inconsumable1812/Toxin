mixin index-menu(options)
  - const { theme, items = [] } = options || {};

  ul.index-menu(class=`index-menu_theme_${theme}`)
    each link in items
      - const { href, text } = link;
      li.index-menu__item
        a.index-menu__link(href=href)= text
